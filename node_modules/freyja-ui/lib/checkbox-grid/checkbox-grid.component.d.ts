import { EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { FormGroup } from '@angular/forms';
export declare class CheckboxGridComponent implements OnChanges {
    title: string;
    checkboxGroup: any[];
    inline: boolean;
    noPadding: boolean;
    noMargin: boolean;
    smallText: boolean;
    noBorder: boolean;
    formGroup: FormGroup;
    checkboxClicked: EventEmitter<{}>;
    /**
     * @description Internally used checkbox data
     */
    checkboxes: any[];
    constructor();
    ngOnChanges(changes: SimpleChanges): void;
    /**
     * @description Create the internally used array of checkboxes
     */
    private makeCheckboxes;
    /**
     * @description Notify the parent that a value has changed
     * @param checked the new checked value of the checkbox
     * @param item the internal checkbox model
     */
    updateChecked(checked: any, item: any): void;
}
