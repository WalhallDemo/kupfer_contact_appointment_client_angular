import { EventEmitter, OnInit } from '@angular/core';
export declare class ContentSwitcherComponent implements OnInit {
    /**
     * content switcher options
     */
    options: any[];
    /**
     * the selected value of the content switcher
     */
    selectedOption: any;
    /**
     * event triggered when an option is selected
     */
    optionSelected: EventEmitter<any>;
    /**
     * the id of the active tab
     */
    activeTab: number;
    ngOnInit(): void;
    /**
     *
     * @param index - index of the selected element
     * @param value - value of the selected element
     */
    selectOption(index: number, value: number): void;
}
