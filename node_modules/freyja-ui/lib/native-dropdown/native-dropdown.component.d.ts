import { EventEmitter } from '@angular/core';
import { BaseComponent } from '../shared/base.component';
import { FormGroup } from '@angular/forms';
export declare class NativeDropdownComponent extends BaseComponent {
    /**
     * options of the dropdown
     */
    options: any[];
    /**
     * dropdown id
     */
    id: string;
    /**
     * dropdown width
     */
    width: string;
    /**
     * wether or not to show default value after selecting an option
     */
    showDefaultOnCLick: boolean;
    /**
     * event that is triggered when an option is selected
     */
    optionSelected: EventEmitter<any>;
    /**
     * Error Message to be displayed
     */
    error: string;
    /**
     * reactive form control name
     */
    controlName: string;
    /**
     * reactive form group
     */
    formGroup: FormGroup;
    constructor();
    /**
     * function triggered when an option is selected
     * @param evt - mouse click event
     */
    onOptionSelected(evt: any): void;
    /**
     * @param evt - mouse click event
     */
    onDropdownClicked(evt: any): void;
    /**
     * @description Add css classes based on inputs like size, shape etc
     */
    generateClassList(): {
        [x: string]: "medium" | "small" | "micro" | "large";
    };
}
